{
  "_": "Written by AI-Graph on every save, from graph.json: read it, do not edit it. Ports are changed in the editor; the shape of the outputs is output.schema.json, where this node keeps one.",
  "node": "rows",
  "type": "code",
  "label": "Table rows",
  "about": "Puts each file name beside its summary",
  "inputs": [
    {
      "port": "files",
      "name": "Files",
      "type": "any",
      "list": true,
      "description": "Every path in the folder",
      "from": [
        "page.folder_out"
      ]
    },
    {
      "port": "summaries",
      "name": "Summaries",
      "type": "text",
      "list": true,
      "description": "One per file, same order",
      "from": [
        "per_file.output"
      ]
    }
  ],
  "outputs": [
    {
      "port": "rows",
      "name": "Rows",
      "type": "json",
      "description": "A list of {File, Summary}",
      "to": [
        "page.table_in"
      ]
    }
  ],
  "output_schema": {
    "type": "object",
    "properties": {
      "rows": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "File": {
              "type": "string"
            },
            "Summary": {
              "type": "string"
            }
          },
          "required": [
            "File",
            "Summary"
          ]
        }
      }
    },
    "required": [
      "rows"
    ]
  }
}
