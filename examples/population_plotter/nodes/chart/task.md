Read the CSV (names in the first column, values in the first numeric column), keep the largest "top" rows, and say what the chart should show: {kind, title, points}, where kind is "bars", "columns", "line" or "donut" translated from the words the dropdown uses. Do not draw anything — the chart block draws the figure at the size it really is. For a donut, add the rows left over as one "All others" slice. Also return the plotted rows for a table.
