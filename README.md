# AI-Graph
A no-code, node-based orchestration platform designed to empower non-programmers to build complex, AI-driven automation workflows. By using a visual graph interface users can transform inputs into structured outputs through natural language instructions which are either converted to code, or interpreted by an AI.
