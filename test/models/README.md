# Test Models

This folder holds manual test graph models and fixture notes.

- `universal_plotter.json` is configured for local Windows paths:
  - model/output folder: `e:\\test\\universal_plotter`
  - sample data file: `e:\\test\\data.csv`

`data/` is reserved for repo-local sample files when needed.
